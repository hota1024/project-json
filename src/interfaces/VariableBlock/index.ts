import { DataBlockInterface } from '../DataBlock'

/*
 * VariableBlock interface.
 */
export interface VariableBlockInterface extends DataBlockInterface {
  /**
   * Variable ID.
   */
  variableId: string
}
